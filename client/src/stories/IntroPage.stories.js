import React from "react";
import IntroPage from "../page/IntroPage";

export default {
  title: "Game/Intro",
  component: IntroPage,
};

const Template = () => <IntroPage />;

export const GameIntro = Template.bind({});
