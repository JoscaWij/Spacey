import React from "react";
import SpriteAnimation from "./spriteAnimation";

export default {
  title: "Game/Sprites",
  component: SpriteAnimation,
};

const Template = () => <SpriteAnimation />;

export const PlayerAnimation = Template.bind({});
