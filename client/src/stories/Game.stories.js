import React from "react";
import Game from "../game/level1/Game";

export default {
  title: "Game/Game",
  component: Game,
};

const Template = () => <Game />;

export const GamePlay = Template.bind({});
