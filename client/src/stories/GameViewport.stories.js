import React from "react";
import GameViewport from "../game/level1/GameViewport";

export default {
  title: "Game/Camera",
  component: GameViewport,
};

const Template = () => <GameViewport />;

export const GameInklCamera = Template.bind({});
