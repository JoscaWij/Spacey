import React from "react";
import BackToMenuButton from "../components/BackToMenuButton";

export default {
  title: "App/BackToMenuButton",
  component: BackToMenuButton,
};

const Template = () => <BackToMenuButton />;

export const Buttontogoback = Template.bind({});
