import styled from "@emotion/styled";
import React from "react";

const ImageButton = styled.button`
  background: none;
`;

const ProceedButton = () => {
  return <ImageButton>weiter</ImageButton>;
};

export default ProceedButton;
